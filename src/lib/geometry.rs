use crate::vec::Vec3;

mod line;
mod plane;

pub struct Plane {
    point: Vec3,
    normal: Vec3,
}

impl Plane {
    fn new() -> Self {
        todo!();
    }

    fn from_vectors() -> Self {
        todo!();
    }
}
